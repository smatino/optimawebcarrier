/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to purchase Celerio ? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Template pack-backend-jpa:src/test/java/service/RepositoryIT.e.vm.java
 */
package com.optima.opticarrier.repository;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.assertThat;

import java.util.List;
import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import com.optima.opticarrier.domain.SbcWlrResponse;
import com.optima.opticarrier.domain.SbcWlrResponse_;
import com.optima.opticarrier.repository.support.SearchParameters;

/**
 * Integration test on SbcWlrResponseRepository
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class SbcWlrResponseRepositoryIT {
    @SuppressWarnings("unused")
    private static final Logger log = LoggerFactory.getLogger(SbcWlrResponseRepositoryIT.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private SbcWlrResponseRepository sbcWlrResponseRepository;

    @Inject
    private SbcWlrResponseGenerator sbcWlrResponseGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        SbcWlrResponse sbcWlrResponse = sbcWlrResponseGenerator.getSbcWlrResponse();

        // add it to a Set before saving (force equals/hashcode)
        Set<SbcWlrResponse> set = newHashSet(sbcWlrResponse, sbcWlrResponse);
        assertThat(set).hasSize(1);

        sbcWlrResponseRepository.save(sbcWlrResponse);
        entityManager.flush();

        // reload it from cache and check equality
        SbcWlrResponse model = new SbcWlrResponse();
        model.setId(sbcWlrResponse.getId());
        assertThat(sbcWlrResponse).isEqualTo(sbcWlrResponseRepository.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // pk are equals...
        assertThat(sbcWlrResponse.getId()).isEqualTo(sbcWlrResponseRepository.get(model).getId());

        // but since you do not use a business key, equality is lost.
        assertThat(sbcWlrResponse).isNotEqualTo(sbcWlrResponseRepository.get(model));
    }

    @Test
    @Rollback
    public void saveAndGetWithExplicitNullPropertySelector() {
        SbcWlrResponse sbcWlrResponse = sbcWlrResponseGenerator.getSbcWlrResponse();

        // add it to a Set before saving (force equals/hashcode)
        Set<SbcWlrResponse> set = newHashSet(sbcWlrResponse, sbcWlrResponse);
        assertThat(set).hasSize(1);

        // explicitly set toOne relationship to null
        sbcWlrResponse.setFile(null);

        sbcWlrResponseRepository.save(sbcWlrResponse);
        entityManager.flush();

        // clear cache to force reload from db
        entityManager.clear();

        SearchParameters searchParameters = new SearchParameters() //
                .caseInsensitive() //
                .anywhere() //
                .property(SbcWlrResponse_.file, (Object) null);

        List<SbcWlrResponse> elements = sbcWlrResponseRepository.find(searchParameters);
        assertThat(elements).isNotEmpty();
        for (SbcWlrResponse element : elements) {
            assertThat(element.getFile()).isNull();
        }
    }
}