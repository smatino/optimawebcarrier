/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to purchase Celerio ? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Template pack-backend-jpa:src/test/java/service/RepositoryIT.e.vm.java
 */
package com.optima.opticarrier.repository;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.assertThat;

import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import com.optima.opticarrier.domain.CpsDeactivationPrenotification;

/**
 * Integration test on CpsDeactivationPrenotificationRepository
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class CpsDeactivationPrenotificationRepositoryIT {
    @SuppressWarnings("unused")
    private static final Logger log = LoggerFactory.getLogger(CpsDeactivationPrenotificationRepositoryIT.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private CpsDeactivationPrenotificationRepository cpsDeactivationPrenotificationRepository;

    @Inject
    private CpsDeactivationPrenotificationGenerator cpsDeactivationPrenotificationGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        CpsDeactivationPrenotification cpsDeactivationPrenotification = cpsDeactivationPrenotificationGenerator.getCpsDeactivationPrenotification();

        // add it to a Set before saving (force equals/hashcode)
        Set<CpsDeactivationPrenotification> set = newHashSet(cpsDeactivationPrenotification, cpsDeactivationPrenotification);
        assertThat(set).hasSize(1);

        cpsDeactivationPrenotificationRepository.save(cpsDeactivationPrenotification);
        entityManager.flush();

        // reload it from cache and check equality
        CpsDeactivationPrenotification model = new CpsDeactivationPrenotification();
        model.setId(cpsDeactivationPrenotification.getId());
        assertThat(cpsDeactivationPrenotification).isEqualTo(cpsDeactivationPrenotificationRepository.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // pk are equals...
        assertThat(cpsDeactivationPrenotification.getId()).isEqualTo(cpsDeactivationPrenotificationRepository.get(model).getId());

        // but since you do not use a business key, equality is lost.
        assertThat(cpsDeactivationPrenotification).isNotEqualTo(cpsDeactivationPrenotificationRepository.get(model));
    }

}