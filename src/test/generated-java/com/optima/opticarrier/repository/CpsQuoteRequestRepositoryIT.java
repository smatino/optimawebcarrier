/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to purchase Celerio ? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Template pack-backend-jpa:src/test/java/service/RepositoryIT.e.vm.java
 */
package com.optima.opticarrier.repository;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.assertThat;

import java.util.List;
import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import com.optima.opticarrier.domain.CpsQuoteRequest;
import com.optima.opticarrier.domain.CpsQuoteRequest_;
import com.optima.opticarrier.repository.support.SearchParameters;

/**
 * Integration test on CpsQuoteRequestRepository
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class CpsQuoteRequestRepositoryIT {
    @SuppressWarnings("unused")
    private static final Logger log = LoggerFactory.getLogger(CpsQuoteRequestRepositoryIT.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private CpsQuoteRequestRepository cpsQuoteRequestRepository;

    @Inject
    private CpsQuoteRequestGenerator cpsQuoteRequestGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        CpsQuoteRequest cpsQuoteRequest = cpsQuoteRequestGenerator.getCpsQuoteRequest();

        // add it to a Set before saving (force equals/hashcode)
        Set<CpsQuoteRequest> set = newHashSet(cpsQuoteRequest, cpsQuoteRequest);
        assertThat(set).hasSize(1);

        cpsQuoteRequestRepository.save(cpsQuoteRequest);
        entityManager.flush();

        // reload it from cache and check equality
        CpsQuoteRequest model = new CpsQuoteRequest();
        model.setId(cpsQuoteRequest.getId());
        assertThat(cpsQuoteRequest).isEqualTo(cpsQuoteRequestRepository.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // pk are equals...
        assertThat(cpsQuoteRequest.getId()).isEqualTo(cpsQuoteRequestRepository.get(model).getId());

        // but since you do not use a business key, equality is lost.
        assertThat(cpsQuoteRequest).isNotEqualTo(cpsQuoteRequestRepository.get(model));
    }

    @Test
    @Rollback
    public void saveAndGetWithExplicitNullPropertySelector() {
        CpsQuoteRequest cpsQuoteRequest = cpsQuoteRequestGenerator.getCpsQuoteRequest();

        // add it to a Set before saving (force equals/hashcode)
        Set<CpsQuoteRequest> set = newHashSet(cpsQuoteRequest, cpsQuoteRequest);
        assertThat(set).hasSize(1);

        // explicitly set toOne relationship to null
        cpsQuoteRequest.setFile(null);

        cpsQuoteRequestRepository.save(cpsQuoteRequest);
        entityManager.flush();

        // clear cache to force reload from db
        entityManager.clear();

        SearchParameters searchParameters = new SearchParameters() //
                .caseInsensitive() //
                .anywhere() //
                .property(CpsQuoteRequest_.file, (Object) null);

        List<CpsQuoteRequest> elements = cpsQuoteRequestRepository.find(searchParameters);
        assertThat(elements).isNotEmpty();
        for (CpsQuoteRequest element : elements) {
            assertThat(element.getFile()).isNull();
        }
    }
}