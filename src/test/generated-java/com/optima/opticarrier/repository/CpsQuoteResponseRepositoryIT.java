/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to purchase Celerio ? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Template pack-backend-jpa:src/test/java/service/RepositoryIT.e.vm.java
 */
package com.optima.opticarrier.repository;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.assertThat;

import java.util.List;
import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import com.optima.opticarrier.domain.CpsQuoteResponse;
import com.optima.opticarrier.domain.CpsQuoteResponse_;
import com.optima.opticarrier.repository.support.SearchParameters;

/**
 * Integration test on CpsQuoteResponseRepository
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class CpsQuoteResponseRepositoryIT {
    @SuppressWarnings("unused")
    private static final Logger log = LoggerFactory.getLogger(CpsQuoteResponseRepositoryIT.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private CpsQuoteResponseRepository cpsQuoteResponseRepository;

    @Inject
    private CpsQuoteResponseGenerator cpsQuoteResponseGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        CpsQuoteResponse cpsQuoteResponse = cpsQuoteResponseGenerator.getCpsQuoteResponse();

        // add it to a Set before saving (force equals/hashcode)
        Set<CpsQuoteResponse> set = newHashSet(cpsQuoteResponse, cpsQuoteResponse);
        assertThat(set).hasSize(1);

        cpsQuoteResponseRepository.save(cpsQuoteResponse);
        entityManager.flush();

        // reload it from cache and check equality
        CpsQuoteResponse model = new CpsQuoteResponse();
        model.setId(cpsQuoteResponse.getId());
        assertThat(cpsQuoteResponse).isEqualTo(cpsQuoteResponseRepository.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // pk are equals...
        assertThat(cpsQuoteResponse.getId()).isEqualTo(cpsQuoteResponseRepository.get(model).getId());

        // but since you do not use a business key, equality is lost.
        assertThat(cpsQuoteResponse).isNotEqualTo(cpsQuoteResponseRepository.get(model));
    }

    @Test
    @Rollback
    public void saveAndGetWithExplicitNullPropertySelector() {
        CpsQuoteResponse cpsQuoteResponse = cpsQuoteResponseGenerator.getCpsQuoteResponse();

        // add it to a Set before saving (force equals/hashcode)
        Set<CpsQuoteResponse> set = newHashSet(cpsQuoteResponse, cpsQuoteResponse);
        assertThat(set).hasSize(1);

        // explicitly set toOne relationship to null
        cpsQuoteResponse.setOlo(null);

        cpsQuoteResponseRepository.save(cpsQuoteResponse);
        entityManager.flush();

        // clear cache to force reload from db
        entityManager.clear();

        SearchParameters searchParameters = new SearchParameters() //
                .caseInsensitive() //
                .anywhere() //
                .property(CpsQuoteResponse_.olo, (Object) null);

        List<CpsQuoteResponse> elements = cpsQuoteResponseRepository.find(searchParameters);
        assertThat(elements).isNotEmpty();
        for (CpsQuoteResponse element : elements) {
            assertThat(element.getOlo()).isNull();
        }
    }
}