/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to purchase Celerio ? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Template pack-backend-jpa:src/test/java/service/RepositoryIT.e.vm.java
 */
package com.optima.opticarrier.repository;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.assertThat;

import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import com.optima.opticarrier.domain.Prefissi;

/**
 * Integration test on PrefissiRepository
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class PrefissiRepositoryIT {
    @SuppressWarnings("unused")
    private static final Logger log = LoggerFactory.getLogger(PrefissiRepositoryIT.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private PrefissiRepository prefissiRepository;

    @Inject
    private PrefissiGenerator prefissiGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        Prefissi prefissi = prefissiGenerator.getPrefissi();

        // add it to a Set before saving (force equals/hashcode)
        Set<Prefissi> set = newHashSet(prefissi, prefissi);
        assertThat(set).hasSize(1);

        prefissiRepository.save(prefissi);
        entityManager.flush();

        // reload it from cache and check equality
        Prefissi model = new Prefissi();
        model.setId(prefissi.getId());
        assertThat(prefissi).isEqualTo(prefissiRepository.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // since you use a business key, equality must be preserved.
        assertThat(prefissi).isEqualTo(prefissiRepository.get(model));
    }

}