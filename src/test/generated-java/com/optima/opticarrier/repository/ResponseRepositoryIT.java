/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to purchase Celerio ? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Template pack-backend-jpa:src/test/java/service/RepositoryIT.e.vm.java
 */
package com.optima.opticarrier.repository;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.assertThat;

import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import com.optima.opticarrier.domain.Response;

/**
 * Integration test on ResponseRepository
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class ResponseRepositoryIT {
    @SuppressWarnings("unused")
    private static final Logger log = LoggerFactory.getLogger(ResponseRepositoryIT.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private ResponseRepository responseRepository;

    @Inject
    private ResponseGenerator responseGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        Response response = responseGenerator.getResponse();

        // add it to a Set before saving (force equals/hashcode)
        Set<Response> set = newHashSet(response, response);
        assertThat(set).hasSize(1);

        responseRepository.save(response);
        entityManager.flush();

        // reload it from cache and check equality
        Response model = new Response();
        model.setId(response.getId());
        assertThat(response).isEqualTo(responseRepository.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // pk are equals...
        assertThat(response.getId()).isEqualTo(responseRepository.get(model).getId());

        // but since you do not use a business key, equality is lost.
        assertThat(response).isNotEqualTo(responseRepository.get(model));
    }

}