/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to purchase Celerio ? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Template pack-backend-jpa:src/test/java/service/RepositoryIT.e.vm.java
 */
package com.optima.opticarrier.repository;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.assertThat;

import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import com.optima.opticarrier.domain.SbcFileResponse;

/**
 * Integration test on SbcFileResponseRepository
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class SbcFileResponseRepositoryIT {
    @SuppressWarnings("unused")
    private static final Logger log = LoggerFactory.getLogger(SbcFileResponseRepositoryIT.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private SbcFileResponseRepository sbcFileResponseRepository;

    @Inject
    private SbcFileResponseGenerator sbcFileResponseGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        SbcFileResponse sbcFileResponse = sbcFileResponseGenerator.getSbcFileResponse();

        // add it to a Set before saving (force equals/hashcode)
        Set<SbcFileResponse> set = newHashSet(sbcFileResponse, sbcFileResponse);
        assertThat(set).hasSize(1);

        sbcFileResponseRepository.save(sbcFileResponse);
        entityManager.flush();

        // reload it from cache and check equality
        SbcFileResponse model = new SbcFileResponse();
        model.setId(sbcFileResponse.getId());
        assertThat(sbcFileResponse).isEqualTo(sbcFileResponseRepository.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // pk are equals...
        assertThat(sbcFileResponse.getId()).isEqualTo(sbcFileResponseRepository.get(model).getId());

        // but since you do not use a business key, equality is lost.
        assertThat(sbcFileResponse).isNotEqualTo(sbcFileResponseRepository.get(model));
    }

}