/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to purchase Celerio ? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Template pack-backend-jpa:src/test/java/service/RepositoryIT.e.vm.java
 */
package com.optima.opticarrier.repository;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.assertThat;

import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import com.optima.opticarrier.domain.CpsDeactivationNotification;

/**
 * Integration test on CpsDeactivationNotificationRepository
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class CpsDeactivationNotificationRepositoryIT {
    @SuppressWarnings("unused")
    private static final Logger log = LoggerFactory.getLogger(CpsDeactivationNotificationRepositoryIT.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private CpsDeactivationNotificationRepository cpsDeactivationNotificationRepository;

    @Inject
    private CpsDeactivationNotificationGenerator cpsDeactivationNotificationGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        CpsDeactivationNotification cpsDeactivationNotification = cpsDeactivationNotificationGenerator.getCpsDeactivationNotification();

        // add it to a Set before saving (force equals/hashcode)
        Set<CpsDeactivationNotification> set = newHashSet(cpsDeactivationNotification, cpsDeactivationNotification);
        assertThat(set).hasSize(1);

        cpsDeactivationNotificationRepository.save(cpsDeactivationNotification);
        entityManager.flush();

        // reload it from cache and check equality
        CpsDeactivationNotification model = new CpsDeactivationNotification();
        model.setId(cpsDeactivationNotification.getId());
        assertThat(cpsDeactivationNotification).isEqualTo(cpsDeactivationNotificationRepository.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // pk are equals...
        assertThat(cpsDeactivationNotification.getId()).isEqualTo(cpsDeactivationNotificationRepository.get(model).getId());

        // but since you do not use a business key, equality is lost.
        assertThat(cpsDeactivationNotification).isNotEqualTo(cpsDeactivationNotificationRepository.get(model));
    }

}