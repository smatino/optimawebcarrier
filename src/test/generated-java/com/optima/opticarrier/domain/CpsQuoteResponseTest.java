/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to purchase Celerio ? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Template pack-backend-jpa:src/test/java/domain/ModelTest.e.vm.java
 */
package com.optima.opticarrier.domain;

import static org.fest.assertions.Assertions.assertThat;

import java.io.*;
import java.util.*;

import org.junit.Test;

import com.optima.opticarrier.util.ValueGenerator;

/**
 * Basic tests for CpsQuoteResponse
 */
@SuppressWarnings("unused")
public class CpsQuoteResponseTest {

    // test unique primary key
    @Test
    public void newInstanceHasNoPrimaryKey() {
        CpsQuoteResponse model = new CpsQuoteResponse();
        assertThat(model.isIdSet()).isFalse();
    }

    @Test
    public void isIdSetReturnsTrue() {
        CpsQuoteResponse model = new CpsQuoteResponse();
        model.setId(ValueGenerator.getUniqueInteger());
        assertThat(model.getId()).isNotNull();
        assertThat(model.isIdSet()).isTrue();
    }

    //-------------------------------------------------------------
    // Many to One:  CpsQuoteResponse.file ==> SbcFileResponse.id
    //-------------------------------------------------------------

    @Test
    public void manyToOne_setFile() {
        CpsQuoteResponse many = new CpsQuoteResponse();

        // init
        SbcFileResponse one = new SbcFileResponse();
        one.setId(ValueGenerator.getUniqueInteger());
        many.setFile(one);

        // make sure it is propagated properly
        assertThat(many.getFile()).isEqualTo(one);

        // now set it to back to null
        many.setFile(null);

        // make sure null is propagated properly
        assertThat(many.getFile()).isNull();
    }

    @Test
    public void manyToOne_setOlo() {
        CpsQuoteResponse many = new CpsQuoteResponse();

        // init
        OloCps one = new OloCps();
        one.setId(ValueGenerator.getUniqueInteger());
        many.setOlo(one);

        // make sure it is propagated properly
        assertThat(many.getOlo()).isEqualTo(one);

        // now set it to back to null
        many.setOlo(null);

        // make sure null is propagated properly
        assertThat(many.getOlo()).isNull();
    }

    /*
     public void equalsUsingPk() {
     CpsQuoteResponse model1 = new CpsQuoteResponse();
     CpsQuoteResponse model2 = new CpsQuoteResponse();

     Integer id = ValueGenerator.getUniqueInteger();
     model1.setId(id);
     model2.setId(id);

     model1.setCodiceArea("ddddd");
     model2.setCodiceArea("ddddd");

     model1.setDescrizioneArea("dddddddddddddddddddddddddddddd");
     model2.setDescrizioneArea("dddddddddddddddddddddddddddddd");

     model1.setQuantitaRichesta(1);
     model2.setQuantitaRichesta(1);

     model1.setVolumiMensiliAssegnati(1);
     model2.setVolumiMensiliAssegnati(1);

     model1.setTolleranzaMensile(1);
     model2.setTolleranzaMensile(1);

     model1.setVolumiGiornalieriAssegnati(1);
     model2.setVolumiGiornalieriAssegnati(1);

     model1.setTolleranzaGiornaliera(1);
     model2.setTolleranzaGiornaliera(1);

     model1.setGiorniLavorativi(1);
     model2.setGiorniLavorativi(1);
     assertThat(model1.isIdSet()).isTrue();
     assertThat(model2.isIdSet()).isTrue();
     assertThat(model1.hashCode()).isEqualTo(model2.hashCode());
     assertThat(model1).isEqualTo(model2);
     assertThat(model2).isEqualTo(model1);
     }
     */
}